import Header from '../lib/header' // This the header element that we can add to each page
import styles from '../styles/Home.module.css' // Hear is the css for the Home page


const Home = () => {
    return (
        <Header pageTitle={'Home'}></Header>
    )
}
export default Home